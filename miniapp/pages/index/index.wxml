<!--pages/index/index.wxml - 收银台-->
<view class="cashier-page">
  <!-- 顶部状态栏 -->
  <view class="top-bar">
    <view class="top-bar-left">
      <text class="top-title">收银台</text>
    </view>
    <view class="top-bar-right">
      <view class="conn-status {{serverConnected ? 'connected' : 'disconnected'}}">
        <view class="conn-dot"></view>
        <text>{{serverConnected ? '已连接' : '未连接'}}</text>
      </view>
    </view>
  </view>

  <!-- 搜索/扫码区域 -->
  <view class="search-section">
    <view class="search-bar">
      <view class="search-input-wrap">
        <text class="search-icon">🔍</text>
        <input
          class="search-input"
          placeholder="商品名称/条码"
          value="{{searchQuery}}"
          bindinput="onSearchInput"
          bindconfirm="doSearch"
          confirm-type="search"
          disabled="{{searching}}"
        />
        <view class="search-loading" wx:if="{{searching}}">
          <view class="loading-spinner"></view>
        </view>
      </view>
      <view class="scan-btn" bindtap="scanBarcode">
        <text class="scan-btn-icon">📷</text>
        <text class="scan-btn-label">扫码</text>
      </view>
      <view class="scan-btn {{scanMode ? 'scan-btn-active' : 'scan-btn-continuous'}}" bindtap="toggleScanMode">
        <text class="scan-btn-icon">{{scanMode ? '⏹' : '📹'}}</text>
        <text class="scan-btn-label">{{scanMode ? '停止' : '连扫'}}</text>
      </view>
    </view>

    <!-- 持续扫码相机区域 -->
    <view class="continuous-camera-wrap" wx:if="{{scanMode}}">
      <camera
        wx:if="{{cameraReady}}"
        class="continuous-camera"
        mode="scanCode"
        resolution="medium"
        flash="{{flashMode}}"
        bindscancode="onScanCode"
        binderror="onCameraError"
      >
        <cover-view class="camera-scan-overlay">
          <cover-view class="camera-scan-frame">
            <cover-view class="corner corner-tl"></cover-view>
            <cover-view class="corner corner-tr"></cover-view>
            <cover-view class="corner corner-bl"></cover-view>
            <cover-view class="corner corner-br"></cover-view>
            <cover-view class="scan-line"></cover-view>
          </cover-view>
          <cover-view class="camera-scan-tip">对准条码自动添加</cover-view>
        </cover-view>
      </camera>
      <view class="camera-toolbar">
        <view class="camera-tool-item" bindtap="toggleFlash">
          <text class="tool-icon">{{flashMode === 'on' ? '🔦' : '💡'}}</text>
          <text class="tool-text">闪光灯</text>
        </view>
        <view class="camera-tool-item close-item" bindtap="toggleScanMode">
          <text class="tool-icon">⏹</text>
          <text class="tool-text">关闭</text>
        </view>
      </view>
    </view>

    <!-- 手动添加按钮 -->
    <view class="manual-add-btn" bindtap="openManualAdd">
      <text class="manual-add-icon">✏️</text>
      <text class="manual-add-label">手动添加</text>
    </view>

    <!-- 添加成功提示 -->
    <view class="add-toast" wx:if="{{lastAddedItem}}">
      <text>✅ {{lastAddedItem}}</text>
    </view>
  </view>

  <!-- 购物车区域 -->
  <view class="cart-section">
    <view class="cart-header">
      <text class="cart-title">购物车</text>
      <text class="cart-count" wx:if="{{totalItems > 0}}">{{totalItems}} 件</text>
      <text class="cart-clear" wx:if="{{cartItems.length > 0}}" bindtap="clearCart">清空</text>
    </view>

    <!-- 购物车列表 -->
    <scroll-view class="cart-list" scroll-y wx:if="{{cartItems.length > 0}}">
      <view class="cart-item" wx:for="{{cartItems}}" wx:key="barcode">
        <view class="item-info">
          <text class="item-name">{{item.name}}</text>
          <text class="item-barcode">{{item.barcode}}</text>
        </view>
        <view class="item-controls">
          <view class="qty-control">
            <view class="qty-btn minus" data-index="{{index}}" bindtap="decreaseQty">
              <text>-</text>
            </view>
            <text class="qty-value">{{item.quantity}}</text>
            <view class="qty-btn plus" data-index="{{index}}" bindtap="increaseQty">
              <text>+</text>
            </view>
          </view>
          <view class="item-price-col">
            <text class="item-unit-price">¥{{item.price}}</text>
            <text class="item-subtotal">¥{{item.subtotal}}</text>
          </view>
        </view>
        <view class="item-delete" data-index="{{index}}" bindtap="removeItem">
          <text>✕</text>
        </view>
      </view>
    </scroll-view>

    <!-- 空购物车 -->
    <view class="cart-empty" wx:if="{{cartItems.length === 0}}">
      <text class="empty-icon">🛒</text>
      <text class="empty-text">购物车为空</text>
      <text class="empty-hint">扫描条码或搜索商品添加</text>
    </view>
  </view>

  <!-- 底部结算栏 -->
  <view class="checkout-bar">
    <view class="checkout-info">
      <text class="checkout-label">合计：</text>
      <text class="checkout-amount">¥{{totalAmount}}</text>
    </view>
    <view
      class="checkout-btn {{cartItems.length === 0 || checkingOut ? 'disabled' : ''}}"
      bindtap="checkout"
    >
      <text>{{checkingOut ? '结账中...' : '结账'}}</text>
    </view>
  </view>

  <!-- 手动添加商品弹窗 -->
  <view class="manual-add-overlay" wx:if="{{showManualAdd}}" bindtap="closeManualAdd">
    <view class="manual-add-card" catchtap="noop">
      <view class="manual-add-header">
        <text class="manual-add-title">手动添加商品</text>
        <text class="manual-add-close" bindtap="closeManualAdd">✕</text>
      </view>

      <view class="manual-add-form">
        <view class="form-item">
          <text class="form-label">商品名称</text>
          <input
            class="form-input"
            placeholder="如：鸡蛋、苹果（选填，默认称重商品）"
            value="{{manualName}}"
            bindinput="onManualNameInput"
          />
        </view>
        <view class="form-item">
          <text class="form-label">金额（元）</text>
          <input
            class="form-input price-input"
            type="digit"
            placeholder="请输入金额"
            value="{{manualPrice}}"
            bindinput="onManualPriceInput"
            bindconfirm="confirmManualAdd"
            focus="{{showManualAdd}}"
          />
        </view>
      </view>

      <view class="manual-add-actions">
        <view class="btn-cancel" bindtap="closeManualAdd">取消</view>
        <view class="btn-confirm" bindtap="confirmManualAdd">添加到购物车</view>
      </view>
    </view>
  </view>

  <!-- 未配置服务器提示 -->
  <view class="no-server-overlay" wx:if="{{!serverUrl}}">
    <view class="no-server-card">
      <text class="no-server-icon">⚙️</text>
      <text class="no-server-title">请先配置服务器</text>
      <text class="no-server-desc">前往设置页面配置服务器地址后即可使用收银功能</text>
      <navigator url="/pages/settings/settings" open-type="switchTab" class="btn-go-settings">
        前往设置
      </navigator>
    </view>
  </view>
</view>
